# (x-1)(x+1) = x^2 - 2x - 1
    a = 1.0;
    b = -1.0;
    c = -2.0;
    discriminant = sqrt(b * b - a * c * 4.0);
    x1 = (0.0-b + discriminant) / (2*a);
    x2 = (0.0-b - discriminant) / (2*a);

    print (x1);
    print (x2);
#    print("x1 = ", x1, "\n");
#    print("x2 = ", x2, "\n");
