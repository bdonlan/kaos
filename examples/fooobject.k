install {
        agent a;

        a = newsimple(1, 1, 100, "oval", 1, 0, 100);
        a.attributes = 128 | 64 | 2 | 1;
        move(a, 211, 196);
        # boolean pls?
        a.dcor = 1;
        a.perm = 32 | 16 | 2 | 1;
}
